// missile.c -- one form of bullet
#include <ncurses.h>
#define LENGTH_MISSILE 5
void missile(int, int);
void missile(int missile_y, int missile_x)
{
	int y, x;
	for (y = 1; y <= LENGTH_MISSILE; i++)
		for (x = 1; x <= 
}
